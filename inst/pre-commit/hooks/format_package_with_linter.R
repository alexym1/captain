#!/usr/bin/env Rscript

cli::cli_h1("Format package with linter")

#summary_linter <- lint_package(
#  linters = linters_with_defaults(semicolon_linter = semicolon_linter()),
#  exclusions = list("inst/doc/creating_linters.R" = 1, "inst/example/bad.R")
#)
