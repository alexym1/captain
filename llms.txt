# captain [![](reference/figures/sticker.png)](#)

> Running R pre-commit hooks

## Overview

Pre-commit hooks are scripts that run automatically before a commit is
finalized in Git. They’re used to catch issues early by enforcing checks
like code formatting, linting, or running tests before changes are
committed.

`captain` (hook) is a package that allows you to run git pre-commit
hooks in a R environment.

## Installation

The `captain` package can be installed from CRAN as follows:

``` r
install.packages("captain")
```

The latest version can be installed from GitHub as follows:

``` r
# install.packages("pak")
pak::pak("alexym1/captain")
```

## Usage

### Initialize pre-commit framework

``` r
captain::install_precommit()
```

### Run hooks

``` r
captain::run_precommit()
```

### Add hooks

Editing the `.pre-commit-config` file using
[`captain::edit_precommit_config()`](https://alexym1.github.io/captain/reference/create_precommit_config.md):

``` bash
repos:
  - repo: local
    hooks:
      - id: renv
        name: Synchronize project from renv.lock
        description: Synchronize the project from the renv.lock
        entry: Rscript inst/pre-commit/hooks/synchronize_project.R
        language: system
        pass_filenames: false
```

## Code of conduct

Please note that this project is released with a [Contributor Code of
Conduct](https://alexym1.github.io/captain/CONTRIBUTING.html). By
participating in this project you agree to abide by its terms.

## Acknowledgments

This logo was created by
[@obstacle.graphic](https://linktr.ee/obstacle.graphic).

# Package index

## Install

Install pre-commit in the repo

- [`install_precommit()`](https://alexym1.github.io/captain/reference/install_precommit.md)
  : Install pre-commit

## Configure

Handle .pre-commit-config file.

- [`create_precommit_config()`](https://alexym1.github.io/captain/reference/create_precommit_config.md)
  [`edit_precommit_config()`](https://alexym1.github.io/captain/reference/create_precommit_config.md)
  : Handle .pre-commit-config file
- [`create_precommit_hook()`](https://alexym1.github.io/captain/reference/create_precommit_hook.md)
  : Create pre-commit hooks
- [`toggle_precommit_hook()`](https://alexym1.github.io/captain/reference/toggle_precommit_hook.md)
  : Toggle pre-commit hooks

## Run

Run pre-commit hooks.

- [`run_precommit()`](https://alexym1.github.io/captain/reference/run_precommit.md)
  : Run precommit

# Articles

### All vignettes

- [Introduction to
  captain](https://alexym1.github.io/captain/articles/captain.md):

  Start here if this is your first time using captain.
